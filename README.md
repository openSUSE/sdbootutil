Tool to manage systemd-boot and grub2-bls in a btrfs based, snapper
managed system.

Can install systemd-boot with shim as well as install kernels into
the ESP.

A non-interactive mode can be called from scriptlets or triggers.
